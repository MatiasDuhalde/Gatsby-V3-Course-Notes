import React from 'react';
import { PageProps } from 'gatsby';
import Layout from '../components/Layout';

const TagsPage: React.FC<PageProps> = () => {
  return (
    <Layout>
      <h2>This is the tags page</h2>
    </Layout>
  );
};

export default TagsPage;
